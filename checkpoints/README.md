# Model checkpoints

Checkpoint files are stored in the GitHub Release named checkpoints-v1.

Download them with:

gh release download checkpoints-v1 --repo DidarTito/VideoClassificationTest --dir checkpoints
